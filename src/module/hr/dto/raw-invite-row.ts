export class RawInviteRow {
  email?: string;
  name?: string;
  dob?: string | number | Date;
  department?: string;
  roleRaw?: string;
  address?: string;
  sex?: string;
  phone?: string;
  startDate?: string | number | Date;
}

// file này để hứng dữ liệu từ excel
