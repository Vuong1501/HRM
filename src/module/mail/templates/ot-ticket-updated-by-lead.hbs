<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <style>
    body { font-family: Arial, sans-serif; background: #f4f4f4; margin: 0; padding: 0; }
    .container { max-width: 600px; margin: 40px auto; background: #fff; border-radius: 8px; overflow: hidden; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
    .header { background: #6a1b9a; padding: 24px; text-align: center; }
    .header h1 { color: #fff; margin: 0; font-size: 20px; }
    .body { padding: 32px; color: #333; }
    .info-box { background: #f3e5f5; border-left: 4px solid #6a1b9a; padding: 16px; border-radius: 4px; margin: 20px 0; }
    .info-row { margin: 8px 0; font-size: 15px; }
    .label { font-weight: bold; color: #555; }
    .badge { display: inline-block; background: #6a1b9a; color: #fff; padding: 4px 12px; border-radius: 12px; font-size: 13px; }
    .reason-box { background: #fff8e1; border-left: 4px solid #f9a825; padding: 12px 16px; border-radius: 4px; margin: 16px 0; font-size: 15px; }
    .footer { background: #f4f4f4; text-align: center; padding: 16px; font-size: 12px; color: #999; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>✏️ Thời gian OT đã được cập nhật</h1>
    </div>
    <div class="body">
      <p>Xin chào <strong>{{employeeName}}</strong>,</p>
      <p>Báo cáo OT của bạn đã được <span class="badge">LEAD CẬP NHẬT</span></p>
      <div class="info-box">
        <div class="info-row"><span class="label">👤 Nhân viên:</span> {{employeeName}}</div>
        <div class="info-row"><span class="label">🕐 Giờ check-in mới:</span> {{checkInTime}}</div>
        <div class="info-row"><span class="label">🕐 Giờ check-out mới:</span> {{checkOutTime}}</div>
        <div class="info-row"><span class="label">⏱ Tổng thời gian:</span> {{actualMinutes}} phút</div>
        <div class="info-row"><span class="label">📝 Nội dung công việc:</span> {{workContent}}</div>
        <div class="info-row"><span class="label">💼 Hình thức:</span> {{mode}}</div>
        {{#if modeChanged}}
        <div class="info-row" style="color: #e65100;">
          <span class="label">⚠️ Lưu ý:</span> Mode đã tự động chuyển sang OT do không đủ thời gian nghỉ bù
        </div>
        {{/if}}
      </div>
      <p><strong>Lý do cập nhật:</strong></p>
      <div class="reason-box">{{updateReason}}</div>
      <p>Nếu có thắc mắc, vui lòng liên hệ trực tiếp với lead của bạn.</p>
      <p>Trân trọng,<br/><strong>Hệ thống quản lý nhân sự</strong></p>
    </div>
    <div class="footer">Email này được gửi tự động, vui lòng không trả lời.</div>
  </div>
</body>
</html>